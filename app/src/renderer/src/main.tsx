import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import 'tldraw/tldraw.css'   // keep this

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
